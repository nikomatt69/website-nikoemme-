---
---

<section
  class="relative pt-32 pb-24 md:pt-44 md:pb-36 overflow-hidden flex flex-col justify-center min-h-[92vh]"
>
  <div class="absolute inset-0 grid-pattern -z-20 opacity-60"></div>
  <div
    class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[500px] bg-terminal-accent/10 dark:bg-terminal-accent/15 blur-[120px] rounded-full pointer-events-none -z-10 animate-pulse-slow mix-blend-screen"
  >
  </div>

  <div class="container mx-auto px-4 relative z-10">
    <div
      class="max-w-4xl mx-auto flex flex-col items-center text-center animate-fade-in-up"
    >
      <a
        href="https://github.com/nikomatt69/nikcli"
        target="_blank"
        rel="noreferrer"
        class="inline-flex items-center gap-2 px-3.5 py-1.5 rounded-full border border-terminal-border bg-surface-hover hover:bg-terminal-panel hover:shadow-soft transition-all duration-300 mb-10 text-[13px] font-semibold text-terminal-text group shadow-sm active:scale-95"
      >
        <span class="relative flex h-2.5 w-2.5">
          <span
            class="animate-ping absolute inline-flex h-full w-full rounded-full bg-terminal-accent opacity-75"
          ></span>
          <span
            class="relative inline-flex rounded-full h-2.5 w-2.5 bg-terminal-accent"
          ></span>
        </span>
        <span
          class="text-terminal-muted group-hover:text-terminal-text transition-colors"
          >Currently building <span class="font-bold text-terminal-text">nikcli</span></span
        >
        <svg
          class="w-4 h-4 text-terminal-muted group-hover:text-terminal-text group-hover:translate-x-0.5 transition-all"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2.5"
            d="M9 5l7 7-7 7"></path>
        </svg>
      </a>

      <h1
        class="text-[3rem] sm:text-[4rem] md:text-[5.5rem] font-black tracking-[-0.03em] text-terminal-text mb-8 leading-[1.02] drop-shadow-sm"
      >
        Hi, I'm <span class="font-mono text-terminal-accent">Niko</span>.
        <br />
        I build
        <span
          class="bg-gradient-to-r from-terminal-accent via-teal-400 to-blue-500 bg-clip-text text-transparent relative"
        >
          AI&nbsp;tools
          <span
            class="absolute inset-x-0 bottom-0 h-[20%] bg-terminal-accent/10 blur-xl -z-10"
          ></span>
        </span>
        for developers.
      </h1>

      <p
        class="text-lg md:text-xl text-terminal-muted mb-12 max-w-2xl leading-relaxed font-medium"
      >
        Full-stack engineer obsessed with terminal-native UX, AI agents and
        beautiful, opinionated developer tools. I ship open source and treat
        every commit like a craft.
      </p>

      <div
        class="w-full max-w-3xl mx-auto mb-14 shadow-strong rounded-[20px] overflow-hidden border border-terminal-window-border/80 dark:border-white/10 bg-terminal-window-bg text-left transform transition-transform hover:scale-[1.02] duration-500 hover:shadow-glow relative group"
      >
        <div
          class="flex items-center justify-between px-5 py-3.5 bg-gradient-to-b from-surface-hover to-terminal-window-bg border-b border-terminal-window-border/60"
        >
          <div class="flex gap-2">
            <div
              class="w-3 h-3 rounded-full bg-[#FF5F56] border border-black/10 dark:border-white/10 shadow-sm"
            >
            </div>
            <div
              class="w-3 h-3 rounded-full bg-[#FFBD2E] border border-black/10 dark:border-white/10 shadow-sm"
            >
            </div>
            <div
              class="w-3 h-3 rounded-full bg-[#27C93F] border border-black/10 dark:border-white/10 shadow-sm"
            >
            </div>
          </div>
          <div
            class="text-[12px] font-mono font-medium text-terminal-muted flex items-center gap-2 px-3 py-1 rounded-md bg-surface-active/50 border border-terminal-border/30"
          >
            <svg
              class="w-3.5 h-3.5 opacity-70"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2.5"
                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
              ></path>
            </svg>
            ~/niko
          </div>
          <div class="w-12"></div>
        </div>

        <div
          class="p-6 md:p-8 font-mono text-[14px] md:text-[15px] leading-relaxed bg-terminal-window-code overflow-x-auto min-h-[180px] relative"
        >
          <div
            class="absolute inset-0 shadow-[inset_0_4px_15px_rgba(0,0,0,0.02)] pointer-events-none"
          >
          </div>

          <div class="space-y-3 relative z-10">
            <div class="flex items-start gap-3">
              <span class="text-terminal-accent shrink-0 select-none font-bold mt-0.5"
                >❯</span
              >
              <div class="flex-1 whitespace-nowrap">
                <span id="typewriter-cmd" class="text-terminal-window-text font-bold"
                ></span>
                <span id="typewriter-flags" class="text-terminal-muted"></span>
                <span
                  class="animate-cursor inline-block w-2.5 h-5 bg-terminal-text align-middle ml-1 -mt-0.5 shadow-[0_0_8px_rgba(var(--terminal-text)/0.5)] rounded-sm"
                ></span>
              </div>
            </div>
            <div id="output" class="hidden pl-6 space-y-1 text-terminal-muted">
              <div>
                <span class="text-terminal-accent">name</span>:
                <span class="text-terminal-window-text">Niko (nikomatt69)</span>
              </div>
              <div>
                <span class="text-terminal-accent">role</span>:
                <span class="text-terminal-window-text">AI Engineer · Builder</span>
              </div>
              <div>
                <span class="text-terminal-accent">stack</span>:
                <span class="text-terminal-window-text"
                  >TypeScript · Astro · Next · Cloudflare</span
                >
              </div>
              <div>
                <span class="text-terminal-accent">status</span>:
                <span class="text-green-500">available for collabs</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-5 w-full">
        <a
          href="#projects"
          class="w-full sm:w-auto inline-flex items-center justify-center px-8 py-3.5 text-[15px] font-bold rounded-xl bg-terminal-text text-terminal-bg hover:bg-terminal-text/90 hover:-translate-y-0.5 transition-all shadow-[0_4px_14px_0_rgb(var(--terminal-shadow)/0.2)] hover:shadow-[0_6px_20px_rgba(var(--terminal-shadow)/0.3)] active:translate-y-0 active:scale-95 group relative overflow-hidden"
        >
          <span
            class="absolute inset-0 bg-white/20 dark:bg-black/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out"
          ></span>
          <span class="relative">See my work</span>
        </a>
        <a
          href="#contact"
          class="w-full sm:w-auto inline-flex items-center justify-center px-8 py-3.5 text-[15px] font-bold rounded-xl bg-surface-hover text-terminal-text hover:bg-terminal-panel border border-terminal-border/80 hover:border-terminal-accent/50 transition-all shadow-sm hover:shadow-soft hover:-translate-y-0.5 active:translate-y-0 active:scale-95"
        >
          Get in touch
        </a>
      </div>
    </div>
  </div>
</section>

<script>
  const cmdText = "whoami";
  const flagsText = " --json";

  const cmdEl = document.getElementById("typewriter-cmd");
  const flagsEl = document.getElementById("typewriter-flags");
  const outputEl = document.getElementById("output");

  let i = 0;
  let j = 0;
  const prefersReduced =
    window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  function typeWriter() {
    if (i < cmdText.length && cmdEl) {
      cmdEl.textContent = (cmdEl.textContent || "") + cmdText.charAt(i);
      i++;
      setTimeout(typeWriter, 60);
    } else if (j < flagsText.length && flagsEl) {
      flagsEl.textContent = (flagsEl.textContent || "") + flagsText.charAt(j);
      j++;
      setTimeout(typeWriter, 40);
    } else if (outputEl) {
      setTimeout(() => outputEl.classList.remove("hidden"), 400);
    }
  }

  if (cmdEl) cmdEl.textContent = "";
  if (flagsEl) flagsEl.textContent = "";

  if (cmdEl && flagsEl) {
    if (prefersReduced) {
      cmdEl.textContent = cmdText;
      flagsEl.textContent = flagsText;
      outputEl?.classList.remove("hidden");
    } else {
      setTimeout(typeWriter, 800);
    }
  }
</script>
